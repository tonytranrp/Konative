package androidx.core.viewtree

// Build-mechanism shim - see androidx/lifecycle/runtime/R.kt's comment for the full explanation.
// Field determined by decompiling androidx.core.viewtree.ViewTree.getParentOrViewTreeDisjointParent
// (the exact method R8's "missing class" error cited) rather than guessed.
class R {
    object id {
        const val view_tree_disjoint_parent: Int = 0x7f0d0005
    }
}
